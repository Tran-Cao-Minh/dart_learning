import 'package:flutter/material.dart';

class HomeOrderChart extends StatelessWidget {
  const HomeOrderChart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
